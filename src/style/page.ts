import css from 'styled-jsx/css';

export const pageWrapper = css`
    .pageWrapper {
        max-width: 1024px;
        margin: 0 auto;
        min-height: calc(100vh - 49px);
        padding: 20px 0;
    }
`;